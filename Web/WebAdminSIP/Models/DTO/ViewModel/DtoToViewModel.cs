using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace WebAdminSIP.Models.DTO.ViewModel
{
    public class DtoToViewModel
    {
        //public List<Prestacion> ToDTO_Prestacion(List<Prestacion> itemList)
        //{
        //    List<Prestacion> prestacionList = new List<Prestacion>();

        //    foreach (Prestacion item in itemList)
        //    {

        //        Prestacion pr = new Prestacion();

        //pr.

        //        pa.idFolio = Convert.ToInt64(item.idFolio);
        //        pa.rutPago = Convert.ToInt32(item.rutPago);
        //        pa.montoPago = Convert.ToInt32(item.montoPago);
        //        pa.estadoPago = item.estadoPago;
        //        pa.fechaPago = Convert.ToDateTime(item.fechaPago);
        //        pa.tipoPago = item.tipoPago;
        //        pa.medioPago = Convert.ToInt32(item.medioPago);
        //        pa.isapre = item.isapre;

        //        pagoList.Add(pr);
        //    }

        //    return prestacionList;
        //}

        //public List<Prestador> ToDTO_Prestador(List<Prestador> itemList)
        //{
        //    List<Prestador> prestadorList = new List<Prestador>();

        //    foreach (Prestador item in itemList)
        //    {

        //        Prestador pr = new Prestador();

        //pr.

        //        pa.idFolio = Convert.ToInt64(item.idFolio);
        //        pa.rutPago = Convert.ToInt32(item.rutPago);
        //        pa.montoPago = Convert.ToInt32(item.montoPago);
        //        pa.estadoPago = item.estadoPago;
        //        pa.fechaPago = Convert.ToDateTime(item.fechaPago);
        //        pa.tipoPago = item.tipoPago;
        //        pa.medioPago = Convert.ToInt32(item.medioPago);
        //        pa.isapre = item.isapre;

        //        pagoList.Add(pr);
        //    }

        //    return prestadorList;
        //}

        //public List<Solicitud> ToDTO_Solicitud(List<Solicitud> itemList)
        //{
        //    List<Solicitud> prestadorList = new List<Solicitud>();

        //    foreach (Prestador item in itemList)
        //    {

        //        Prestador pr = new Prestador();

        //pr.

        //        pa.idFolio = Convert.ToInt64(item.idFolio);
        //        pa.rutPago = Convert.ToInt32(item.rutPago);
        //        pa.montoPago = Convert.ToInt32(item.montoPago);
        //        pa.estadoPago = item.estadoPago;
        //        pa.fechaPago = Convert.ToDateTime(item.fechaPago);
        //        pa.tipoPago = item.tipoPago;
        //        pa.medioPago = Convert.ToInt32(item.medioPago);
        //        pa.isapre = item.isapre;

        //        pagoList.Add(pr);
        //    }

        //    return prestadorList;
        //}
    }
}